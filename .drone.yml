clone:
  clone:
    image: plugins/git
    recursive: true

pipeline:
  semantic-release:
    image: node:8-alpine
    commands:
      - git --version
      - npm install --save-dev semantic-release
      - npm install @semantic-release/git -Dt
      - npx semantic-release